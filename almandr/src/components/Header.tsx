const Header = () => {
  return (
    <header className="h-14 flex items-center justify-between px-6 border-b bg-white">
      <span className="text-sm font-semibold tracking-wide">ALMANDR</span>

      <span className="text-xs text-gray-500">Admin Dashboard</span>
    </header>
  );
};

export default Header;
